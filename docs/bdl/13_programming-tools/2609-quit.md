---
title: "quit"
source: "fgl-topics/c_fgl_Debugger_quit.html"
breadcrumb: "Programming tools > Integrated debugger > Debugger commands > quit"
type: "concept"
---

# quit

> The quit command terminates the debugger session.

## Syntax

```
quit
```

## Usage

The `quit` command
allows you to exit the debugger.

`q` is an alias
for the `quit` command.

## Example

```
(fgldb) quit
```
