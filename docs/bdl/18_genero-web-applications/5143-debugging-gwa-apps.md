---
title: "Debugging GWA apps"
source: "fgl-topics/c_fgl_gwa_debugging.html"
breadcrumb: "Genero Web applications > Debugging GWA apps"
type: "concept"
---

# Debugging GWA apps

> Different solutions are available to debug a GWA application.

[`gwadb`](5151-gwadb.md "The gwadb is a debug tool that allows you to debug GWA applications generated by gwabuildtool in the browser on your desktop.") is the tool provided for
debugging GWA applications. For example, to start a debug session in a directory where you have a
program called main.4gl, you must first compile your program and then run the
debug command:

```
$ gwadb main
```

Internally, this command will call [`gwabuildtool`](5150-gwabuildtool.md "The gwabuildtool is a utility to build a GWA application with all the necessary files to run on a browser.") to build the
application package in the gwa\_dist directory and [gwasrv](5152-helper-tools.md "The gwasrv is a mini web server written in Genero that allows you to run applications generated by gwabuildtool in the browser on your desktop.") will be called to serve your application in the browser.

Your application opens in the default browser with an address like this:

(Line breaks have
been added to improve readability.)

```
http://localhost:9101/d/r/index.html
             ?fglapp=main&uuid=gwarun2025_01_31_10_18_58_835
             &debug=1&debugSSE=1
             &viaMiniWS=1&t=2025_01_31_10_19_00_309&update=1
```

A loading page
is displayed while waiting for you to start debugging.

![Screenshot of loading page when starting to debug a GWA application](../_images/gwadb_browser_screen.png)

*GWA loading*

In the terminal, the [fgldb](../13_programming-tools/2520-fgldb.md "The fgldb tool is an interface program for remote debugging.")
debugging tool is running in attached mode to your program, ready to run fgldb
commands like `run`, `break`, `step`,
`continue`, `quit`, and so on.

```
gwadb main
gwadb:Wait for debuggee.....running fgldb on port:9400
(fgldb) run
```

For more details about using fgldb tool commands and debugging, go to [Starting fglrun in debug mode](../13_programming-tools/2576-starting-fglrun-in-debug-mode.md "The runtime system can be started in debug mode with the -d option.") or type help at the prompt.
> **Note:**
>
> **Genero Studio graphical
> debugger**
>
> If using Genero Studio, the graphical debugger may be used. For more details, refer
> to the Packaging for GWA and Graphical Debugger topics in the Genero Studio User Guide.

After quitting the debugger, the browser window should be marked as closed like this:

![Screenshot of GWA application page after closing the debugger](../_images/gwadb_browser_close_page.png)

*GWA page closing debugger*

## Example: debug demo

The GWA installation includes a debug demo. To run the demo:

1. Locate the debug demo. If GWA was installed in FGLDIR, you will find the demo in
   $FGLDIR/demo/debug or if installed in a separate directory, you will find it in
   gwa-install-dir/demo/debug
2. If you have make installed, you can run the Makefile
   included with the demo, which internally compiles the source files and builds the application
   package in the gwa\_dist directory before running the gwadb
   command:

   ```
   make gwa.debug
   ```

   Or if you also want to build the distribution files,
   run this target in the
   makefile:

   ```
   make gwa.debug_build
   ```

   Both commands, start the
   application in the browser and open fgldb in the terminal.
3. If not using make, you must compile the demo program first and then run the
   debug command. For example, If you run the gwadb command with the module to
   debug, it builds the gwa\_dist directory
   internally:

   ```
   gwadb main
   ```

   Or you can run the gwadb command
   with the home page of the application after calling [`gwabuildtool`](5150-gwabuildtool.md "The gwabuildtool is a utility to build a GWA application with all the necessary files to run on a browser.") to build the application package in the default
   gwa\_dist directory:

   ```
   gwabuildtool -p .
   gwadb gwa_dist/index.html
   ```

   Both commands, start the application in the browser and
   open fgldb in the terminal for debugging.

## Related links

**Related concepts**  

[Troubleshooting GWA apps](5142-troubleshooting-gwa-apps.md "What steps can you take if you have trouble with a Genero web application (GWA)?")
