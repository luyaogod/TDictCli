---
title: "gwadb"
source: "fgl-topics/c_fgl_tools_gwa_gwadb.html"
breadcrumb: "Genero Web applications > GWA Reference > Tools and commands > gwadb"
type: "concept"
---

# gwadb

> The gwadb is a debug tool that allows you to debug GWA applications generated by gwabuildtool in the browser on your desktop.

## Syntax

```
gwadb {  module_name[.42m] | homepage }
```

1. module\_name is the name of the program module which the command is to debug.
2. homepage is the name of the main HTML page of your application. Typically,
   this is index.html

## Usage

To run gwadb, you need to have it in your PATH (see [Install Genero Web Application](../04_installation/0053-install-genero-web-application.md "To build and package Genero Web applications, you must first install Genero Web Application (GWA).")).

For example, to start a debug session in a directory where you have a program called
main.4gl, you must first compile your program and then run the debug
command:

```
$ gwadb main
```

Internally, this command will call [`gwabuildtool`](5150-gwabuildtool.md "The gwabuildtool is a utility to build a GWA application with all the necessary files to run on a browser.") to build the
application package in the gwa\_dist directory and [gwasrv](5152-helper-tools.md "The gwasrv is a mini web server written in Genero that allows you to run applications generated by gwabuildtool in the browser on your desktop.") will be called to serve your application in the browser.

If you have already built your application package with `gwabuildtool`, you can
run the gwadb command with the home page in the default
gwa\_dist directory:

```
gwabuildtool -p .
gwadb gwa_dist/index.html
```

The application will open in the browser. In the terminal, the
[fgldb](../13_programming-tools/2520-fgldb.md "The fgldb tool is an interface program for remote debugging.") debugging tool will be running
in attached mode to your program, where you can run fgldb commands.

```
gwadb main
gwadb:Wait for debuggee.....running fgldb on port:9400
(fgldb) run
```

For more details about using fgldb tool commands and debugging, go to [Starting fglrun in debug mode](../13_programming-tools/2576-starting-fglrun-in-debug-mode.md "The runtime system can be started in debug mode with the -d option.") or type help at the prompt.

For more examples, go to [Debugging GWA apps](5143-debugging-gwa-apps.md "Different solutions are available to debug a GWA application.").

## Related links

**Related concepts**  

[gwabuildtool](5150-gwabuildtool.md "The gwabuildtool is a utility to build a GWA application with all the necessary files to run on a browser.")

[gwarun](5152-helper-tools.md "The gwarun tool runs a GWA program in the browser on your desktop.")
