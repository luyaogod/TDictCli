---
title: "base.MessageServer.create"
source: "fgl-topics/c_fgl_ClassMessageServer_create.html"
breadcrumb: "Library reference > Built-in packages > The base package > The MessageServer class > base.MessageServer methods > base.MessageServer.create"
type: "concept"
---

# base.MessageServer.create

> This method is deprecated and does nothing.

## Syntax

> **Warning:**
>
> **Security Note:**
>
> Improper use of the `base.MessageServer` can lead to a denial-of-service (DoS)
> attack. Consider using a solution based on [`ON
> IDLE`](../11_user-interface/1897-on-idle-block.md) to achieve inter-process communication.

> **Important:**
>
> This feature is an empty shell. The syntax or
> API call is still available, but it does nothing.

```
base.MessageServer.create()
```
