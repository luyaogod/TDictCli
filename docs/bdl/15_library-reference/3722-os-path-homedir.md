---
title: "os.Path.homeDir"
source: "fgl-topics/c_fgl_ext_os_path_homedir.html"
breadcrumb: "Library reference > Extension packages > The os package > The os.Path class > os.Path methods > os.Path.homeDir"
type: "concept"
---

# os.Path.homeDir

> Returns the path to the HOME directory of the current user.

## Syntax

```
os.Path.homeDir()
  RETURNS STRING
```

## Usage

This function returns the home directory of the current user.
