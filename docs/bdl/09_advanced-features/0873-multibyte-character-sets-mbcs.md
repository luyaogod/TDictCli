---
title: "Multibyte character sets (MBCS)"
source: "fgl-topics/c_fgl_localization_010.html"
breadcrumb: "Advanced features > Localization > Application locale > Locale and character set basics > Multibyte character sets (MBCS)"
type: "concept"
description: "A multibyte character set defines the encoding for characters on a variable number of bytes. The size of a character can be one (usually ASCII chars), two, three or more bytes, depending on the ..."
---

# Multibyte character sets (MBCS)

A multibyte character set defines the encoding for characters on a variable number of bytes.
The size of a character can be one (usually ASCII chars), two, three or more bytes, depending
on the character set.

Example of multibyte character sets are BIG5, EUC-JP, and UTF-8. BIG5 and EUC-JP characters
can be one or two bytes long, while UTF-8 characters can be 1, 2, 3 or 4 bytes long (usually
a maximum of 3 is sufficient).

Genero Business Development Language supports multibyte character sets.

## Related links

**Related concepts**  

[Length semantics settings](0881-length-semantics-settings.md "Length semantics settings")

[Single-byte character sets (SBCS)](0871-single-byte-character-sets-sbcs.md "Single-byte character sets (SBCS)")

[Double-byte character sets (DBCS)](0872-double-byte-character-sets-dbcs.md "Double-byte character sets (DBCS)")
