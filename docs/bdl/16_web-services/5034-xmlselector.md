---
title: "XMLSelector"
source: "fgl-topics/c_gws_XML_attribute_XMLSelector.html"
breadcrumb: "Web services > Reference > XML serialization rules and customization > XML serialization attributes > XMLSelector"
type: "concept"
description: "Define the index of the candidate among all members of an XMLChoice record that will be serialized or deserialized at runtime. The index starts at 1. The selector data type must be a SMALLINT or a ..."
---

# XMLSelector

Define the index of the candidate among all members of an [XMLChoice](5029-xmlchoice.md) record that
will be serialized or deserialized at runtime.

The index starts at 1.

The selector data type must be a SMALLINT or a INTEGER.
