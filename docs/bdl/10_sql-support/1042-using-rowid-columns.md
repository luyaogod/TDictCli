---
title: "Using ROWID columns"
source: "fgl-topics/c_fgl_sql_programming_090.html"
breadcrumb: "SQL support > SQL programming > SQL portability > Using ROWID columns"
type: "concept"
---

# Using ROWID columns

> Automatic ROWID columns is not a common database feature.

Rowids are implicit primary keys generated by the database engine.
Not all database servers support rowids:

| Database Server Type | Rowid keyword | Rowid type | ROWID support |
| --- | --- | --- | --- |
| IBM® Informix®Starting with Informix 15, when using large tables, ROWID are 8 byte integers (`BIGINT`) | `ROWID` | `INTEGER` or `BIGINT` | Yes, [native SQL feature](1186-what-are-the-supported-ibm-informix-sql-features.md) |
| Microsoft™ SQL Server | No | N/A | No, [see details](1280-rowid-columns.md) |
| Oracle® MySQL / MariadDB | No | N/A | No, [see details](1332-rowid-columns.md) |
| Oracle Database Server | `ROWID` | `CHAR(18)` | Yes, [see details](1378-rowid-columns.md) |
| PostgreSQL | `No` | internal type | No, [see details](1435-rowid-columns.md) |
| SQLite | `ROWID` | `BIGINT` | Yes, [see details](1484-rowid-columns.md) |
| Dameng® | `ROWID` | `CHAR(18)` | Yes, [see details](1233-rowid-columns.md) |

Informix fills the `sqlca.sqlerrd[6]`
register with the `ROWID` of the last updated row. The `sqlerrd[]`
array is defined with an integer type cannot be filled with rowids having
`CHAR(size)` type.

Search for `ROWID` and `sqlca.sqlerrd[6]` in your code and review
the code to remove the usage of rowids.

Instead of using `ROWID` pseudo columns, consider to define `PRIMARY
KEY` constraints in all your database tables, and use the primary key values instead of
rowids to identify rows. To fill the primary key value when inserting a new row, use a [`SEQUENCE`](1026-solution-3-use-native-sequence-database-objects.md).

## Related links

**Related concepts**  

[The sqlca diagnostic record](0988-the-sqlca-diagnostic-record.md "The sqlca variable is a predefined record containing SQL statement execution information.")
