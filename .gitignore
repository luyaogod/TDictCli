# 构建与打包产物
/dist/
*.exe
/web/node_modules/
# go:embed 依赖:clone 后需 cd web && npm install && npm run build
/web/dist/

# 运行时产物
debug-bps/
*.log
.tdict-serve.json

# 数据文件(110MB,可用 tdict db sync 重建)
erp_data.db

# 本地源码镜像(tdict mirror pull;数百 MB,镜像根若设在仓库内需忽略)
erp-src/

# 本地配置(含生产 SSH/数据库明文凭据,切勿提交)
config.json

# 会话数据
.zcode/
tdict.exe~

# db sync 备份(每次同步自动生成)
*.bak
